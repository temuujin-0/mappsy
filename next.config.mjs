/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your Next.js configuration here
  output: 'standalone', // Example config
  // ...
};

export default nextConfig; // Энэ мөрийг ингэж өөрчилнө